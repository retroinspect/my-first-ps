// 통나무 건너뛰기
// 값을 기준으로 한 binary search 하면 될 듯

// 차이의 최댓값의 최솟값
int getMinCost()
{
  int start; // 반드시 가능한 최댓값을 향해 가도록 증가시킴
  int end; // 절대로 불가능한 최솟값을 향해 가도록 감소시킴
  while(true)
  {

  }
}

// 공유기 문제와 똑같은데 x에 대해 -x 에도 점이 존재하고 N개만 채우면 됨

// arr[i] + value 이하 arr[i] - value 이상인 숫자에 화살표를 쏠 때
// 전체 graph에서 모든 정점을 포함하는 cycle을 만들수 있으면 
bool isPossible(int value)
{


}